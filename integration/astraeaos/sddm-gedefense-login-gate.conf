# STATUS: DIAMANT VGT SUPREME
[Unit]
# Keep fail-closed boot: SDDM waits for the gate script to succeed once.
# The gate itself only Wants= GeDefense units so later access/control restarts
# do not deactivate the gate (and therefore do not stop SDDM / Plasma).
Requires=astraeaos-security-login-gate.service
After=astraeaos-security-login-gate.service
